# WHOT frontend

Next.js table. Sign in with email or a wallet, then sit.

```bash
cp .env.example .env
# set NEXT_PUBLIC_WHOT_ADDRESS and NEXT_PUBLIC_CDP_PROJECT_ID
npm run dev
```

Open http://localhost:3000
